// The floating treatment every control on the bottom row is cut from. Each one
// is its own object — a single panel made them read as one compound control,
// and they are unrelated decisions.

export const FLOAT = 'rounded-2xl bg-card/85 shadow-xl ring-1 ring-border/60 backdrop-blur-md';
/** Every floating control is this tall, so the row has one baseline. */
export const CONTROL = 'h-8';
/** A dock button, square. */
export const BUTTON = 32;

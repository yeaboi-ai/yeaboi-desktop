<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  For the helpers and for the bundled Python: they inherit the parent's grants
  rather than carrying their own, which is what lets the sidecar bind a
  loopback port and the renderer helper reach a microphone.

  disable-library-validation is for the voice pack. It is pip-installed at runtime
  into ~/.yeaboi and loaded by the bundled interpreter over PYTHONPATH, so its .so
  files carry a different Team ID than the process; under the hardened runtime,
  library validation would refuse to dlopen them.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.app-sandbox</key>
  <false/>
  <key>com.apple.security.inherit</key>
  <true/>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
